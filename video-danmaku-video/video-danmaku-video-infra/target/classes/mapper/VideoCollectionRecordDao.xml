<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.videodanmaku.video.infra.basic.mapper.VideoCollectionRecordDao">

    <resultMap type="com.videodanmaku.video.infra.basic.entity.VideoCollectionRecord" id="VideoCollectionRecordMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="folderId" column="folder_id" jdbcType="BIGINT"/>
        <result property="videoId" column="video_id" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="isDeleted" column="is_deleted" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="VideoCollectionRecordMap">
        select id, user_id, folder_id, video_id, create_time, is_deleted
        from collection_record
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="VideoCollectionRecordMap">
        select id, user_id, folder_id, video_id, create_time, is_deleted
        from collection_record
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="folderId != null">
                and folder_id = #{folderId}
            </if>
            <if test="videoId != null">
                and video_id = #{videoId}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
        </where>
        order by create_time desc
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from collection_record
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="folderId != null">
                and folder_id = #{folderId}
            </if>
            <if test="videoId != null">
                and video_id = #{videoId}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
        </where>
    </select>

    <!--查询收藏夹中的视频ID列表-->
    <select id="queryVideoIdsByFolderId" resultType="java.lang.Long">
        select video_id
        from collection_record
        where folder_id = #{folderId} and is_deleted = #{isDeleted}
        order by create_time desc
    </select>

    <!--查询用户是否已收藏视频-->
    <select id="queryByUserIdAndVideoId" resultMap="VideoCollectionRecordMap">
        select id, user_id, folder_id, video_id, create_time, is_deleted
        from collection_record
        where user_id = #{userId} and video_id = #{videoId} and is_deleted = #{isDeleted}
        limit 1
    </select>

    <!--查询收藏夹中的视频记录-->
    <select id="queryByFolderId" resultMap="VideoCollectionRecordMap">
        select id, user_id, folder_id, video_id, create_time, is_deleted
        from collection_record
        where folder_id = #{folderId} and is_deleted = #{isDeleted}
        order by create_time desc
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into collection_record(user_id, folder_id, video_id, create_time, is_deleted)
        values (#{userId}, #{folderId}, #{videoId}, #{createTime}, #{isDeleted})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update collection_record
        <set>
            <if test="folderId != null">
                folder_id = #{folderId},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <update id="deleteById">
        update collection_record set is_deleted = 1 where id = #{id}
    </update>

</mapper>